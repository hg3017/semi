<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.semie.cook.mapper.CookingMapper">
    <select id="selectAll" resultType="CounselingDTO">
        select * from counseling
    </select>

    <insert id="insertCounsel"  parameterType="CounselingDTO">
        INSERT INTO COUNSELING (COUNSELING_ID, BOARD_ID, COUNSELING_NAME, DESC_DETAIL, CREATER)
        VALUES (
                   COUNSELING_ID_SEQ.NEXTVAL,
                    40,

               )
    </insert>




</mapper>